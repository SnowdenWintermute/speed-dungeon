import { CombatantClass, MonsterType } from "@speed-dungeon/common";
import { ModularCharacter } from "../combatant-models/modular-character";

export function spawnTestCombatantModels() {
  // const skeleton = await
  const test1 = new ModularCharacter("", MonsterType.Scavenger, CombatantClass.Warrior);
}
