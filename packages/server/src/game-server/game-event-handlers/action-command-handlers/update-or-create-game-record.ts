import { SpeedDungeonGame } from "@speed-dungeon/common";

export default async function updateOrCreateGameRecord(game: SpeedDungeonGame) {
  // if no record exists, create the game record
  // determine if the game is over (all parties wiped)
}
