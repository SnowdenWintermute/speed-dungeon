// it("crashed-server active record doesn't block continuation forever", () => {
//   // simulate: active game status written but never refreshed (server crash)
//   // advance time past stale threshold
//   // a user can now create a continuation lobby for that run
// });
