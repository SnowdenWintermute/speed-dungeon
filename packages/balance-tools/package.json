{
  "name": "@speed-dungeon/balance-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only tooling for moving game balance data between the code and spreadsheets",
  "type": "module",
  "license": "ISC",
  "scripts": {
    "checkTs": "npx tsc --noEmit --watch --preserveWatchOutput",
    "sync": "node --experimental-strip-types src/sync-game-data.ts",
    "sync:watch": "node --experimental-strip-types src/watch-game-data.ts"
  },
  "dependencies": {
    "@speed-dungeon/common": "1.0.0",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {}
}
