import { FloorNumber } from "@speed-dungeon/common";

export function determinePerFloorMonsterAgility(monsterEvasionByFloor: Map<FloorNumber, number>) {
  const monsterAgilityByFloor = new Map<FloorNumber, number>();

  // for (const [floorNumber, evasionOnFloor] of monsterEvasionByFloor) {
  //   monsterAgilityByFloor.set(floorNumber, evasionOnFloor);
  // }
}
