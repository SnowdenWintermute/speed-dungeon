import { EntityId } from "../../primatives/index.js";

export const CHARACTER_LEVEL_LADDER = "character-level-ladder:";

export class RankedLadderService {
  async removeEntry(ladderName: string, entryId: EntityId) {}
}
